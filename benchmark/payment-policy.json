{
  "version": "1.2",
  "purpose": "James paid benchmark guardrails. This file is policy metadata only; it contains no wallet secret or signing credential.",
  "network": "base",
  "asset": "USDC",
  "maxPaymentUsd": 0.025,
  "maxSessionUsd": 1.0,
  "maxPaidCalls": 40,
  "maxQuoteAgeSeconds": 900,
  "arbitraryTransfersAllowed": false,
  "rawPrivateKeyAccessibleToAgent": false,
  "requireProviderAllowlist": true,
  "requireLiveQuote": true,
  "requireQuoteAtOrBelowMaxPayment": true,
  "requireCanonicalProviderIdentity": true,
  "requireHttpsEndpoint": true,
  "requireQuoteExpiry": true,
  "requireUniqueNonce": true,
  "requireTaskEndpointRecipientBinding": true,
  "requireAuditRecord": true,
  "dynamicRecipientPolicy": "fail-closed-unless-enrolled",
  "requireRecipientEnrollment": true,
  "recipientDriftBlocksPayment": true,
  "freshQuoteCannotEnrollRecipient": true,
  "failClosed": true,
  "allowedPurpose": "structured-research benchmark only",
  "requiredAuditFields": [
    "taskId",
    "provider",
    "endpoint",
    "payTo",
    "liveQuoteUsd",
    "quotedAt",
    "expiresAt",
    "nonce",
    "settledCostUsd",
    "attempt",
    "latencyMs",
    "pass",
    "transactionId"
  ],
  "activationGate": [
    "signer/wallet keeps keys outside LLM context",
    "owner-controlled spend limits are enforced outside the agent",
    "agent cannot increase limits",
    "agent cannot make arbitrary transfers",
    "Base and USDC restrictions are enforced",
    "provider endpoint and payment recipient allowlists are enforced",
    "x402 V2 dynamic recipients are never auto-enrolled from a fresh 402 quote",
    "recipient drift for an approved endpoint blocks signing until separately re-enrolled outside the LLM execution path",
    "live quote is no older than 15 minutes and has not expired",
    "payment authorization is bound to task, endpoint, provider and recipient",
    "nonce/payment identifier is unique and cannot be replayed",
    "test transaction proves over-limit requests fail closed",
    "test transaction proves unknown recipient requests fail closed",
    "test transaction proves recipient-drift requests fail closed",
    "test transaction proves stale/expired quote requests fail closed",
    "test transaction proves replayed nonce requests fail closed"
  ]
}
