{
  "name": "james-outcome-economics",
  "version": "0.2.0",
  "private": true,
  "description": "Machine-readable provider ranking by expected cost per acceptable outcome.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test",
    "benchmark:preflight": "node benchmark/preflight-quotes.js"
  },
  "engines": {
    "node": ">=20"
  }
}
